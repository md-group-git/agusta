{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {%- apply spaceless %}
        {% if value %}
            {{ value | number_format(2, ',', ' ') }} ₽
        {% else %}
            -
        {% endif %}
    {% endapply -%}
{% endblock %}
